name: Test Action
on: [push]

# jobs:
#   build:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Hello world
#         run: echo "Hello world"

jobs:
  run-my-action:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run my local action
        uses: greyxr/passkey-cicd@main
        with:
          name: "Test"
      - name: Upload report
        uses: actions/upload-artifact@v4
        with:
          name: scan-report
          path: report.html